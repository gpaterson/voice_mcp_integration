pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://alphacephei.com/maven/' }
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "VoiceMCP"
include ':app'
